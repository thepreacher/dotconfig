;;; init-buffers.el --- buffer settings -*- lexical-binding: t -*-
;;; Commentary:

;;; Code:

;; Bufler
(use-package bufler
  :defer
  :bind ("C-x C-b" . bufler))


(provide 'init-buffers)
;;; init-buffers.el ends here
